import LayoutLesson from '../../../../../components/layout/layoutLesson';

# Cup and Handle

A cup and handle is a bullish signal that consists of a strong downtrend, a strong uptrend, and a short weak downtrend. This chart pattern frequently results in a strong uptrend.

Cup and handles take a long time to form so require a lot of patience from the trader.

{image}

export default ({ children }) => (
  <LayoutLesson
    course="tradingAcademy"
    prevChapter="courseOverview"
    nextChapter="courseOverview"
    prevLesson="syllabus"
    nextLesson="goals"
    currentLessonId={1}
  >
    {children}
  </LayoutLesson>
);
