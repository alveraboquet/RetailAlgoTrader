import LayoutLesson from '../../../../../components/layout/layoutLesson';

# Preventing a Margin Call

Understand how margin works. Know your broker's margin call and stop-out levels.

Use stoplosses on all orders (this will be discussed in a later chapter). Create a proper risk management strategy that calculates proper lot sizing (this will be discussed in a later chapter)

export default ({ children }) => (
  <LayoutLesson
    course="tradingAcademy"
    prevChapter="marginTrading"
    nextChapter="tradingBasics"
    prevLesson="marginCall"
    nextLesson="readingChart"
    currentLessonId={18}
  >
    {children}
  </LayoutLesson>
);
